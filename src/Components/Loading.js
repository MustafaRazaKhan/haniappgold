import { ActivityIndicator, StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Loading = () => {
    return (
      <ActivityIndicator size="large" color="#2E69A8" />
      )
}

export default Loading